import { ThemeProvider } from '../components/skip-selection/ThemeProvider';
import SkipSelectionPage from '../components/SkipSelectionPage';

const Index = () => {
    return (
        <ThemeProvider>
            <SkipSelectionPage />
        </ThemeProvider>
    );
};

export default Index;
